# Mate Codex Handoff R83

## 작업 요약

- 관심 카테고리 전면 개편: 5그룹 × 3개 = 15개 항목으로 재구성.
  - 관람 (L1): 영화 / 공연·전시 / 스포츠 직관
  - 휴식 (L2): 퇴근 후 맥주 / 페스티벌 / 나들이
  - 탐방 (L2): 맛집 / 카페 / 산책
  - 운동 (L2): 러닝 / 등산 / 자전거
  - 자기개발 (L2): 독서 / 스터디 / 보드게임
- `lib/cards/categories.ts`를 단일 소스로 정리.
  - `CardCategoryOption`에 `group` 필드 추가, `CATEGORY_GROUP_ORDER` / `CARD_CATEGORY_LABELS` / `getCategoriesByGroup()` 추가
  - 그룹은 UI 표시용 라벨. DB/매칭/레벨 게이팅은 기존대로 말단 카테고리 문자열 단위 유지 (스키마 변경 없음)
- 온보딩 폼 하드코딩 카테고리 목록 제거 → 단일 소스 import, 그룹별 3열 그리드 렌더링.
- 온보딩에 "직접 입력" 추가.
  - 최대 12자, `hasCashPaymentPattern` 클라이언트 검사, 빈 값/중복 방지
  - 직접 입력값은 프로필 categories에 저장되지만 카드 카테고리 목록에는 없으므로 구독 매칭에 참여하지 않음 (운영 참고용 — 수요 확인 후 정식 카테고리 승격 채널)
- 카테고리 참조 지점 일괄 갱신: 피드 탭(`CARD_CATEGORY_LABELS` 파생), 카드 작성 템플릿, demo-data, dev-auth, 알림 예시, 테스트.
- `isAdminUser` 보안 수정: `VERCEL_ENV === "production"`에서는 서비스 env 누락/dev 폴백 유저가 admin으로 승격되지 않음 (기존엔 env 누락 시 전원 admin).
- 테스트 34 → 37개 (그룹 구조 검증 3개 추가). typecheck / lint / test / build 전부 통과.

## 주의

- 기존 프로필/카드에 저장된 구 카테고리 문자열("야구 직관", "공연", "전시" 등)은 새 목록과 매칭되지 않는다. 운영 데이터가 생기기 전 MVP 단계라 마이그레이션은 생략했으며, 실데이터 발생 후 개편 시에는 매핑 마이그레이션 필요.

## 다음 작업 후보

- 온보딩 카테고리 그리드를 R62 다크 토큰 기준으로 시각 정리 (현재는 기존 스타일 유지)
- 피드 TopicScroller에 그룹 구분 반영 여부 검토
- 직접 입력 관심사 운영 대시보드 노출 (수요 파악용)
